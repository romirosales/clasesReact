import Nav from "./Nav";

const Header = (props) => {
    return (
            
            <header>
                <h1> Trabajamos con React</h1>
            </header>
    
    )
}
export default Header;